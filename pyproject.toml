[project]
name = "describe-it"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Christophe Pettus", email = "cpettus@pgexperts.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
describe-it = "describe_it:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
