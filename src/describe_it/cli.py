"""Command-line entry point for the `describe-it` console script.

A stub for now: the console script is declared in pyproject.toml so packaging
is settled from the first commit, but the argparse front end lands with the
CLI work unit, once there is a describer for it to drive.
"""


def main() -> int:  # pragma: no cover - stub until the CLI work unit
    """Run the describe-it command line.

    Returns:
        The process exit status.
    """
    raise NotImplementedError
